# Pinned stack for the amygdala false-floor reanalysis (Python 3.9).
# numpy MUST stay <2 (torch 2.2.2 / from_numpy compatibility).
numpy==1.26.4
scipy>=1.10,<1.14
scikit-learn>=1.3,<1.7
torch==2.2.2
torchvision==0.17.2
nibabel>=5,<6
nilearn>=0.10,<0.13
opencv-python==4.10.0.84
Pillow>=10
transformers>=4.40           # audio arm (audeering wav2vec2)
# antspyx==0.6.3             # only if re-registering MNI masks from raw (not needed for derivatives)
