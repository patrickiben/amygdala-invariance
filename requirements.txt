# Core (required for the synthetic pipeline + tests + the NAc tSNR audit). Light-weight.
numpy>=1.24
scipy>=1.10
PyYAML>=6.0
pytest>=7.4
nibabel>=5.0            # NAc tSNR pre-flight audit + BOLD masking (laptop-runnable, no GPU)

# Real-data path (optional; the adapters in amyg_inv/data + encoders are IMPLEMENTED but UNTESTED
# on real data — uncomment as you run each one on a compute box).
# nilearn>=0.10         # masking, resampling, confound handling
# himalaya>=0.4         # production banded ridge + variance partitioning (GPU-optional)
# torch>=2.1            # EmoNet / DINOv2 / CLIP feature extraction
# torchvision>=0.16     # EmoNet (AlexNet backbone), image transforms
# open_clip_torch>=2.24 # CLIP ViT features
# transformers>=4.40    # facebook/dinov2-large
# opencv-python>=4.9    # film frame extraction + motion energy
